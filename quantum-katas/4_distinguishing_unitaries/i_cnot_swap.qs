namespace Kata {
    operation DistinguishTwoQubitUnitaries (unitary : (Qubit[] => Unit is Adj + Ctl)) : Int {
        // now it is allowed to use operator twice
        // we can start from |10> state to divide space of possible operations by two time and then repeat previous solutions
        return -1;
    }
}
